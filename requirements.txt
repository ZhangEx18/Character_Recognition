# ============================================================
# CNN 字符识别系统 - 核心环境依赖清单 (requirements.txt)
#
# 【安装指令】
# 请在激活了虚拟环境 (venv) 的终端中运行以下命令进行一键安装：
# pip install -r requirements.txt
# ============================================================

# ------------------------------------------------------------
# 1. 核心深度学习框架 (系统的“大脑”与数学引擎)
# ------------------------------------------------------------
# PyTorch 核心库：负责构建神经网络(CNN/ResNet)、张量计算与自动求导。
# (注：>=2.0.0 版本完美原生支持 Mac M系列芯片的 MPS 硬件加速)
torch>=2.0.0

# 计算机视觉官方扩展包：提供 transforms 模块，负责图像的裁剪、旋转、标准化等数据增强流水线。
torchvision>=0.15.0

# 训练监控仪表盘：在后台记录训练过程，让我们可以在浏览器中实时查看 Loss 和准确率的变化曲线。
tensorboard>=2.13.0

# 科学计算基础库：所有图像和张量在底层都是 Numpy 数组。在混淆矩阵统计中也起到了关键作用。
numpy>=1.24.0

# ------------------------------------------------------------
# 2. 图像处理与视觉采集层 (系统的“眼睛”与制图工具)
# ------------------------------------------------------------
# OpenCV (代码中 import cv2)：工业级计算机视觉库。
# 负责在 tools/camera_app.py 中吊起物理摄像头、渲染 UI 引导框，以及执行 Otsu 二值化清洗。
opencv-python>=4.8.0

# Pillow (PIL)：Python 事实上的图像处理标准库。
# 充当桥梁，将硬盘里的图片或 OpenCV 截取的画面转换成 PyTorch 能够吞吐的格式。
Pillow>=10.0.0

# 科学绘图画板：强大的可视化图表库。
# 负责生成训练结果图、混淆矩阵热力图、特征图显微镜以及最易错字符排行榜。
matplotlib>=3.7.0

# ------------------------------------------------------------
# 3. 命令行工程化辅助工具 (提升开发者体验)
# ------------------------------------------------------------
# 进度条组件 (taqaddum)：
# 在装载数万张图片 (dataset.py) 或执行盲测阅卷 (eval_matrix.py) 时，
# 提供极具科技感的实时进度条和剩余时间预估，彻底告别终端“假死”焦虑。
tqdm>=4.65.0