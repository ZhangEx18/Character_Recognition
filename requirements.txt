# 核心深度学习框架 (Mac MPS / CPU 均适用)
torch>=2.0.0
torchvision>=0.15.0
tensorboard>=2.13.0
numpy>=1.24.0
# 图像处理与可视化
opencv-python>=4.8.0  # 这就是 cv2 对应的包！
Pillow>=10.0.0
matplotlib>=3.7.0

# 辅助工具
tqdm>=4.65.0          # 进度条工具